import Home from "../pages/Home";
import Editor from "../pages/Editor";

export const routes = [
    {
        path: "/",
        component: Home,
    },
    {
        path: "/editor",
        component: Editor,
    }
]